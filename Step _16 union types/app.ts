// union types that a variable can assign that value what we want to assign it

var a : number | number[];
//for example the above variable can only assign number or numbers array

a = 10;
console.log(a);
a = [10,2];
console.log(a);


//add(55);